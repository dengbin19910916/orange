spring:
  application:
    name: product
  cloud:
    config:
      uri: http://localhost:8888
      profile: dev
      discovery:
        enabled: true
        service-id: orange-config-server
server:
  port: 8080